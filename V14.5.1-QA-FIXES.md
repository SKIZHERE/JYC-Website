# V14.5.1 QA + Bug Fix Pass

Date: 2026-09-19

## Verified statically
- JSON syntax: package.json, package-lock.json, vercel.json, public/manifest.json.
- Local source import paths: no missing local module paths detected.
- Local named exports used by imports: no missing named exports detected.
- Public image/service-worker/manifest asset references checked.
- Complete ZIP integrity checked with Python zipfile.testzip().

## Fixed
1. Mobile theme control moved out of the compact header and into a floating circular control below the header, matching the reference layout.
2. Mobile bottom-navigation active pill now sits above the dock background and can visibly slide between tabs.
3. Team counts/search/results no longer expose unpublished team members; published team data uses `published === true`.
4. Deep certificate URLs now work at `/certificates/<certificate-id>`.
5. CSP now permits the optional Spline Phoenix scene and supported campus-map embeds.
6. CSP now permits HTTPS image URLs used by CMS club/event/gallery assets.
7. Brand/favicon/PWA assets now use the square 697×697 JYC logo for crisp small-size rendering; the larger phoenix artwork is used for the hero visual.
8. Service-worker cache key bumped to invalidate stale V14.5 assets.
9. Version bumped to 14.5.1.

## Final verification status
A production Vite build could not be executed in this container because the npm registry package `vite@8.3.0` is not cached and network access to the registry timed out. The source itself was statically checked, but this is not a substitute for a real `npm run build`.

## Known environment limitation
A production Vite build could not be executed in this container because the npm registry package `vite@8.3.0` is not cached and network access to the registry timed out. Static JSX/module/JSON validation passed after the build-breaking import fix.

## Local verification command
```bash
npm install
npm run build
```

Then test on Chrome Android + desktop Chrome/Edge, especially:
- mobile header/theme/search/more sheet
- bottom-nav active pill movement
- hero phoenix interaction
- Clubs / Events / Team filters
- certificate verification deep link
- QR pages
- Spline scene if configured
- campus map if configured
- admin login and role-scoped workspaces
- Fest Mode
- PWA install/offline
